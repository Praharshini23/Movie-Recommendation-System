# Movie-Recommendation-System
This project implements a movie recommendation system which analyzes user watch history to identify frequent movie combinations and generate association rules. These rules are used to recommend movies that are likely to be watched together. The system provides simple, effective, and interpretable recommendations without using complex models.
